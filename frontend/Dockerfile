# /culina-sync/frontend/Dockerfile

# Nutze das offizielle Nginx-Image als Basis
FROM nginx:alpine

# Kopiere den Inhalt des Frontend-Ordners (index.html, app.js etc.)
# in das Verzeichnis, aus dem Nginx standardmäßig Webseiten ausliefert.
COPY . /usr/share/nginx/html